package model;

/**
 * Discharge decision:
 * I (patient sent to Intensive Care Unit),
 * S (patient prepared to go home),
 * A (patient sent to general hospital floor)
 */
public enum DischargeDecision {I, S, A}
